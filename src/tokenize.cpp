#include "tokenize.h"
#include <algorithm>
#include <cctype>

std::vector<Token> Tokenizer::tokenize()
{
    std::vector<Token> tokens;

    while (pos < src.size()) {

        pos++;
    }

    return tokens;
}
